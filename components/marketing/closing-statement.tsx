/**
 * Section 12 — la grande phrase de clôture, pleine largeur.
 * Dégradé d'ambiance (couverture charte). Aucune image.
 */
export function ClosingStatement() {
  return (
    <section className="bg-gradient-ambient py-24 lg:py-40">
      <div className="container-site">
        <p className="font-display text-display-xl max-w-6xl text-balance text-paper">
          Le clienteling, comme vos meilleurs vendeurs le font déjà.
        </p>
      </div>
    </section>
  );
}
